# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: reports.spec.ts >> Reports Flow >> Caso 2: Descargar Excel devuelve el tipo correcto
- Location: tests\e2e\reports.spec.ts:30:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Resumen General')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=Resumen General')

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { createTestUser, deleteTestUser } from './utils/db-setup';
  3  | 
  4  | test.describe('Reports Flow', () => {
  5  |   const adminEmail = 'admin_reports@test.org';
  6  | 
  7  |   test.beforeAll(async () => {
  8  |     await createTestUser(adminEmail, 'SUPER_ADMIN');
  9  |   });
  10 | 
  11 |   test.afterAll(async () => {
  12 |     await deleteTestUser(adminEmail);
  13 |   });
  14 | 
  15 |   // Login once and reuse session via storageState
  16 |   test.beforeEach(async ({ page }) => {
  17 |     await page.goto('/es/login');
  18 |     await page.fill('input[name="email"]', adminEmail);
  19 |     await page.fill('input[name="password"]', 'password123');
  20 |     await page.click('button[type="submit"]');
> 21 |     await expect(page.locator('text=Resumen General')).toBeVisible({ timeout: 10000 });
     |                                                        ^ Error: expect(locator).toBeVisible() failed
  22 |   });
  23 | 
  24 |   test('Caso 1: Descargar PDF devuelve application/pdf', async ({ page }) => {
  25 |     // Use page.request to get the response with the authenticated session cookies
  26 |     const response = await page.request.get('/api/reports/pdf');
  27 |     expect(response.headers()['content-type']).toContain('application/pdf');
  28 |   });
  29 | 
  30 |   test('Caso 2: Descargar Excel devuelve el tipo correcto', async ({ page }) => {
  31 |     const response = await page.request.get('/api/reports/excel');
  32 |     expect(response.headers()['content-type']).toContain('spreadsheetml');
  33 |   });
  34 | });
  35 | 
```