# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: donations.spec.ts >> Donations Flow (RBAC) >> Caso 1: Un TESORERO puede ver el dashboard y acceder a donaciones
- Location: tests\e2e\donations.spec.ts:29:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Resumen General')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=Resumen General')

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { createTestUser, deleteTestUser, clearTestDonations } from './utils/db-setup';
  3  | 
  4  | // Reusable login helper
  5  | async function loginAs(page: any, email: string) {
  6  |   await page.goto('/es/login');
  7  |   await page.fill('input[name="email"]', email);
  8  |   await page.fill('input[name="password"]', 'password123');
  9  |   await page.click('button[type="submit"]');
  10 |   // Dashboard visible = login succeeded
> 11 |   await expect(page.locator('text=Resumen General')).toBeVisible({ timeout: 10000 });
     |                                                      ^ Error: expect(locator).toBeVisible() failed
  12 | }
  13 | 
  14 | test.describe('Donations Flow (RBAC)', () => {
  15 |   const tesoreroEmail = 'tesorero@test.org';
  16 |   const auditorEmail = 'auditor@test.org';
  17 | 
  18 |   test.beforeAll(async () => {
  19 |     await createTestUser(tesoreroEmail, 'TESORERO');
  20 |     await createTestUser(auditorEmail, 'AUDITOR');
  21 |   });
  22 | 
  23 |   test.afterAll(async () => {
  24 |     await deleteTestUser(tesoreroEmail);
  25 |     await deleteTestUser(auditorEmail);
  26 |     await clearTestDonations();
  27 |   });
  28 | 
  29 |   test('Caso 1: Un TESORERO puede ver el dashboard y acceder a donaciones', async ({ page }) => {
  30 |     await loginAs(page, tesoreroEmail);
  31 |     // Verify we are on the dashboard with the correct user context
  32 |     await expect(page.locator('text=Resumen General')).toBeVisible();
  33 |     await expect(page.locator('text=Últimas Donaciones')).toBeVisible();
  34 |   });
  35 | 
  36 |   test('Caso 2: Un AUDITOR ve el dashboard pero no puede crear donaciones', async ({ page }) => {
  37 |     await loginAs(page, auditorEmail);
  38 |     await expect(page.locator('text=Resumen General')).toBeVisible();
  39 |     // AUDITOR should see donations summary but not a "create" button
  40 |     // (RBAC check: form or button to create donation should not be present for AUDITOR)
  41 |     await expect(page.locator('text=Nueva Donación')).not.toBeVisible();
  42 |   });
  43 | });
  44 | 
```